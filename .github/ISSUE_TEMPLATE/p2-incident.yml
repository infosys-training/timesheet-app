name: "P2 Incident - High"
description: "Major feature degraded or unavailable. Workaround may exist but significant user impact."
title: "[P2] "
labels: ["incident", "P2-high"]
assignees: []

body:
  - type: markdown
    attributes:
      value: |
        ## P2 — High Severity Incident
        **SLA**: Acknowledge within **15 minutes**. Tech lead informed within **1 hour**.
        Major feature is degraded or unavailable. Workaround may exist but user impact is significant.

  - type: input
    id: incident-commander
    attributes:
      label: Incident Commander
      description: Who is leading the response?
      placeholder: "@username"
    validations:
      required: true

  - type: dropdown
    id: failure-mode
    attributes:
      label: Failure Mode
      description: Select the primary failure mode (see RUNBOOK.md for details).
      options:
        - "FM-1: Database Failure (SQLite)"
        - "FM-2: API Server Crash / Unresponsive"
        - "FM-3: Authentication & Authorization Errors"
        - "FM-4: Memory Leak / OOM"
        - "FM-5: Dependency / npm Failure"
        - "FM-6: Rate Limiting Blocking Legitimate Users"
        - "FM-7: CSV/PDF Export Failure"
        - "FM-8: Frontend Build / Serving Failure"
        - "FM-9: Docker Container Health Check Failure"
        - "FM-10: Network / CORS Errors"
        - "Other / Unknown"
    validations:
      required: true

  - type: textarea
    id: impact
    attributes:
      label: Impact Assessment
      description: Describe who is affected and how.
      value: |
        **Users affected**: [subset / percentage / specific group]
        **Functionality degraded**: [e.g., CSV export, login for specific users]
        **Workaround available**: [yes / no — describe if yes]
        **Business impact**: [describe]
    validations:
      required: true

  - type: textarea
    id: timeline
    attributes:
      label: Incident Timeline
      description: Record key events with UTC timestamps. Update as incident progresses.
      value: |
        | Time (UTC) | Event |
        |---|---|
        | YYYY-MM-DD HH:MM | Incident detected / reported by [source] |
        | | On-call acknowledged |
        | | Investigation started |
        | | Root cause identified |
        | | Mitigation applied |
        | | Service restored |
    validations:
      required: true

  - type: textarea
    id: description
    attributes:
      label: Incident Description
      description: What happened? Include error messages, logs, and screenshots.
      placeholder: "Describe the incident in detail..."
    validations:
      required: true

  - type: textarea
    id: root-cause
    attributes:
      label: Root Cause
      description: What caused the incident? (Fill in during or after investigation.)
      placeholder: "To be determined during investigation..."
    validations:
      required: false

  - type: textarea
    id: mitigation
    attributes:
      label: Mitigation Steps Taken
      description: What was done to restore or work around the issue?
      placeholder: |
        1. ...
        2. ...
    validations:
      required: false

  - type: textarea
    id: resolution
    attributes:
      label: Resolution & Follow-up
      description: Permanent fix and preventive actions.
      value: |
        **Permanent fix**: [describe or link to PR]
        **Follow-up actions**:
        - [ ] Post-incident review scheduled
        - [ ] Monitoring / alerting improved
        - [ ] Runbook updated
    validations:
      required: false

  - type: textarea
    id: environment
    attributes:
      label: Environment
      description: Where did this occur?
      value: |
        **Environment**: [production / staging / development]
        **Deployment method**: [Docker / bare metal / cloud service]
        **Node.js version**: 
        **Last deploy**: [date/time or commit SHA]
    validations:
      required: true
